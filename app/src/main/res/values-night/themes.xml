<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NebulaMobile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:windowBackground">@drawable/background_wave_drak</item>
        <item name="colorPrimary">@color/PrimaryDark</item>
        <item name="android:navigationBarColor">@color/NavigatorColor</item>
        <item name="android:textColor">@color/TextColorDark</item>
        <item name="CustomShapeColorTransparent">@color/ShapeColorDarkTransparent</item>
        <item name="CustomShapeColor">@color/ShapeColorDark</item>
        <item name="SideNavBar">@drawable/side_nav_dark</item>
    </style>

</resources>